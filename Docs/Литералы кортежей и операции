()                            Пустой кортеж 
T = (0,)                      Кортеж из одного элемента (не выражение) 
T = (0, ‘Ni’, 1.2, 3)         Кортеж из четырех элементов 
T = 0, ‘Ni’, 1.2, 3           Еще один кортеж из четырех элементов  (тот же самый, что и строкой выше)
T = (‘abc’, (‘def’, ‘ghi’))   Вложенные кортежи
T = tuple(‘spam’)             Создание кортежа из итерируемого объекта
T[i]                          Индекс,
T[i][j]                       индекс индекса,
T[i:j]                        срез,
len(T)                        длина
T1 + T2                       Конкатенация,
T * 3                         повторение
for x in T: print(x)          Обход в цикле,
‘spam’ in t2                  проверка вхождения
[x ** 2 for x in T] 
T.index(‘Ni’) T.count(‘Ni’)   Методы кортежей в версиях 2.6 и 3.0: поиск,  подсчет вхождений
